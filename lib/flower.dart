import 'package:flutter/material.dart';

class Flower {
  String name;
  String backgroundImage;
  String flowerImage;
  String description;
  Color darkColor;
  Color lightColor;

  Flower(this.name, this.backgroundImage, this.flowerImage, this.description,
      this.darkColor, this.lightColor);
}
